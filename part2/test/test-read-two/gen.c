#include <stdio.h>
int main() {
  double x, y;
  x = 0;
  x = x;
  scanf("%lf", &x);
  ;
  printf("%g\n", (double)(x + 0.0));
  ;
  scanf("%lf", &y);
  ;
  printf("%g\n", (double)(y + 0.0));
  ;
  return 0;
}
